// import { createSelector } from '@ngrx/store';
// import { AppState } from '../app.state';
// import { DividerState } from './divider.reducer';

// export const selectDivider = (state: AppState) => state.divider;
// export const selectDividerValue = createSelector(
//   selectDivider,
//   (state: DividerState) => state.value
// );
